IssueComment {issueCommentUpdatedAt = GithubDate {fromGithubDate = 2012-11-09 03:12:41 UTC}, issueCommentUser = GithubUser {githubOwnerAvatarUrl = "https://secure.gravatar.com/avatar/6a424256589825f609fd3477144355a0?d=https://a248.e.akamai.net/assets.github.com%2Fimages%2Fgravatars%2Fgravatar-user-420.png", githubOwnerLogin = "yarikoptic", githubOwnerUrl = "https://api.github.com/users/yarikoptic", githubOwnerId = 39889, githubOwnerGravatarId = "6a424256589825f609fd3477144355a0"}, issueCommentUrl = "https://api.github.com/repos/fail2ban/fail2ban/issues/comments/10214300", issueCommentCreatedAt = GithubDate {fromGithubDate = 2012-11-09 03:12:41 UTC}, issueCommentBody = "Hi guys -- first of all thanks for pushing it forward!  \r\n\r\nFirst I have committed a 'crime' -- I have merged it with current master, so now sending pull request against @Th4nat0s master would look really ugly... so I pushed it for now into a separate branch: https://github.com/fail2ban/fail2ban/tree/_tent/ipv6_adapter_cmd so please review commits right there: https://github.com/fail2ban/fail2ban/compare/_tent;ipv6_adapter_cmd  .\r\n\r\nI have \r\n- added some basic unittests for addresses handling\r\n- done minor code refactoring to make it shorter and a bit more Pythonic at few spots (may be even faster? ;) )\r\n- more evil:  \r\n  - renamed fail2ban-iptables into fail2ban-46 and made it a generic dispatcher to switch between two commands provided first on the command line.\r\n  - made iptables-*.conf AND shorewall.conf (now it is possible ;) ) to make use of common.conf where I have defined the 'iptables' and 'shorewall' strings which then replaced previous fail2ban-iptables in those .conf files.\r\n\r\nunfortunately I haven't had yet a chance to test my changes to action files :-/ so they might not work at all... anyone? ;)\r\n\r\nStill on TODO\r\n- we seems to be missing ignoreip handling for ipv6 (thus also underlying cidr etc)\r\n- what is the purpose of 'ipv6' setting in fail2ban.conf ?-- seems to be not used anywhere... and probably we can't really condition fully (i.e. dynamically replace e.g. common.conf with iptables = iptables   configuration).  So I vote to just remove it altogether to not give a false feeling that it is possible to turn its handling  off entirely", issueCommentId = 10214300}