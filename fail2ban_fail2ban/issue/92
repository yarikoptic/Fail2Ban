Issue {issueClosedAt = Nothing, issueUpdatedAt = GithubDate {fromGithubDate = 2012-12-04 14:37:18 UTC}, issueHtmlUrl = "https://github.com/fail2ban/fail2ban/issues/92", issueClosedBy = Nothing, issueLabels = [], issueNumber = 92, issueAssignee = Nothing, issueUser = GithubUser {githubOwnerAvatarUrl = "https://secure.gravatar.com/avatar/9845b46872bbd45362a525f46c740289?d=https://a248.e.akamai.net/assets.github.com%2Fimages%2Fgravatars%2Fgravatar-user-420.png", githubOwnerLogin = "martzuk", githubOwnerUrl = "https://api.github.com/users/martzuk", githubOwnerId = 1249518, githubOwnerGravatarId = "9845b46872bbd45362a525f46c740289"}, issueTitle = "Geo IP", issuePullRequest = PullRequestReference {pullRequestReferenceHtmlUrl = Nothing, pullRequestReferencePatchUrl = Nothing, pullRequestReferenceDiffUrl = Nothing}, issueUrl = "https://api.github.com/repos/fail2ban/fail2ban/issues/92", issueCreatedAt = GithubDate {fromGithubDate = 2012-12-04 14:37:18 UTC}, issueBody = "I followed the tutorial here: http://www.fail2ban.org/wiki/index.php/HOWTO_use_geoiplookup\n\nI can't seem to get it to work, I modified the correct line (I took into account what looks like slightly newer syntax)\n\nHowever when I have this line in the config rather than the original it stops fail2ban from actually banning anybody, also meaning that it doesn't record a ban line in the logfile.\n\nThis is the original line:\n```python\nlogSys.warn(\"[%s] Ban %s\" % (self.jail.getName(), str(aInfo[\"ip\"])))\n```\nThis is the new line: \n```python\nlogSys.warn(\"[%s] Ban %s %s\" % (self.jail.getName(), str(aInfo[\"ip\"], commands.getstatusoutput('geoiplookup ' + aInfo[\"ip\"])[1][23:])))\n```\nI also added commands to the import line so it is: import time, logging, commands\n\nAm I doing something wrong or would this functionality have broken after an update? The little python script at the top of the wiki page seems to work ok.", issueState = "open", issueId = 8985801, issueComments = 0, issueMilestone = Nothing}